# Orchestration Log — Mulder (2026-03-09T22:38)

**Status:** SUCCESS  
**Agent:** Mulder (PowerShell & Diagnostics)  
**Mode:** background, claude-sonnet-4.5

## Directive

Build 10 diagnostic skill files under `skills/` for win-investigator, implementing comprehensive PowerShell-based Windows Server diagnostics.

## Work Completed

### Diagnostic Skills Created (10 total)

1. **connectivity** - WinRM testing, session establishment, credential handling
2. **server-overview** - Hostname, OS, uptime, domain, hardware basics
3. **processes** - Running processes, CPU/memory usage, hung process detection
4. **performance** - CPU, memory, disk I/O metrics with thresholds
5. **disk-storage** - Volume space, disk health, SMART data, large file detection
6. **services** - Service status, crash detection, dependency analysis
7. **installed-apps** - Software inventory, recent installs, hotfixes
8. **network** - IP config, DNS, ports, firewall, connectivity validation
9. **roles-features** - Server roles, IIS/AD DS config, feature inventory
10. **event-logs** - Critical/error events, crash patterns, security events

### Key Patterns Established

- Use `$ServerName` for target server variable
- Use `$Credential` for alternate credentials (null for current user)
- Prefer CIM over WMI: `Get-CimInstance` not `Get-WmiObject`
- All remote calls use `Invoke-Command` with `-ErrorAction Stop` and try/catch
- Return structured objects, not raw text
- Include interpretation tables and error handling examples
- Comprehensive reference documentation in each SKILL.md

### File Locations

All skill files created under `C:\Source\win-investigator\skills/*/SKILL.md` with complete diagnostic instructions.

## Handoff

Work is ready for:
- **Doggett:** Integrate skills into agent instructions
- **Skinner:** Write test scenarios for each skill
