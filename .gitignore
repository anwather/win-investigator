# Squad: ignore runtime state (logs, inbox, sessions)
.squad/orchestration-log/
.squad/log/
.squad/decisions/inbox/
.squad/sessions/
# Squad: SubSquad activation file (local to this machine)
.squad-workstream

# Credential files (stored in $HOME\.wininvestigator\, not in repo, but excluded for safety)
*credentials.xml
*-cred.xml
